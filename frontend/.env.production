# Production build — point at the Render backend URL (no trailing slash).
# Also set VITE_API_BASE_URL in the Vercel project Environment Variables panel
# so builds on Vercel pick it up without committing secrets.
VITE_API_BASE_URL=https://news-credibility-monitor-api.onrender.com
