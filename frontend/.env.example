# Copy to .env.local (or set in Vercel) and fill in values.

# Base URL of the deployed backend. Leave blank in local dev to use the Vite proxy.
VITE_API_BASE_URL=

# Local dev only — target the Vite dev server forwards /analyze to.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
